Reproduce the attached artwork EXACTLY as it is, pixel-faithful: same figure, same pose, same colors, same lettering (if any), same style, same composition, changing NOTHING about the artwork itself. The ONLY change: remove the flat background entirely and output the artwork on a fully TRANSPARENT background (true alpha channel). Soft glows, auras and ink drips at the artwork's edges should fade out via smooth partial transparency instead of fading into a background color. Do not add any new background, color field, vignette, border or shadow. Do not redraw, restyle or "improve" anything.
